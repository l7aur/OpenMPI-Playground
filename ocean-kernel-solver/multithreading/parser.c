#include "parser.h"

#include <stdio.h>
#include <assert.h>
#include <stdlib.h>

int Parser(
    const int argc,
    char * argv[],
    int * 
) {
    assert(argc > 0);
    assert(argv != NULL);

    return EXIT_SUCCESS;
}